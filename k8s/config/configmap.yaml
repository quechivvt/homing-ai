apiVersion: v1
kind: ConfigMap

metadata:
  name: homing-config
  namespace: homing

data:
  GEMINI_MODEL: "gemini-3.6-flash"
  EMBEDDING_MODEL: "models/gemini-embedding-2"

  DB_HOST: "postgres"
  DB_PORT: "5432"
  DB_NAME: "postgres"

  REDIS_URL: "redis://redis:6379"

  PIPELINE: "LANGCHAIN"

  MAX_DISTANCE: "0.4"